\section{Model and Problem Formulation}
\label{sec:formulation}

% 迁移来源：docs/formulation.md（注意符号冲突修正：候选路径数用 $\kappa$，
% active demand 集合用 $\mathcal{K}^t$）

\subsection{Network and Traffic Model}
% [TODO] 迁移 3.1：图/容量/时隙；demand 矩阵 $D^t$；active set $\mathcal{K}^t$；
% 候选路径 $\kappa=4$；split ratio 参数化（$\sum_k r_{i,k} \le 1$，残差=主动
% 不服务）

\subsection{TE with Complete Observation}
% [TODO] 迁移 3.2：LP (1)--(4)；收尾钩子：LP 结构上依赖完整 $D$
% \begin{align} ... \end{align} 公式 (1)-(4)

\subsection{TE with Sparse Observation}
% [TODO] 迁移 3.3：观测模型 $O^t = D^t \odot M$ (5)；flow/node 两种粒度；
% 问题定义 (6)(7)；核心句——决策与评估位于不同信息集

\subsection{Solution Paradigm}
% [TODO] 迁移 3.4：三分法（solve-with-zeros / two-stage / end-to-end）；
% RL 目标 (8)；双轨分离（观测约束输入、真值约束训练信号）
