# 转正答辩文档 0901

# 沙箱平台可观测与可运维能力建设

实习的两项主要工作围绕同一条主线：serving 沙箱（OpenClaw 等长驻 Agent 服务）是**有状态、长生命周期**的生产服务，平台运营需要强化两种基础能力：故障问题**观测追踪**，存量服务**运维升级**。

*   **工作一：Sandbox 全链路 Tracing 建设：**单个 TraceID 贯穿 manager / controller 及上下游组件，跨组件问题从人工对齐日志到分钟级定位
    
*   **工作二：SUO 原地升级与多 SUO 并发建设**：升级不销毁 Pod、数据零搬迁；多次提交的升级确定更新至最新用户意图
    

# 工作1、Sandbox 全链路 Tracing 建设

---

## 一、业务背景与目标

本次工作主要涉及：ACS实例启动链路，休眠唤醒链路。

目前ACS实例休眠唤醒链路很长，涉及到的组件非常多，一次ACS实例的休眠，唤醒，各个环节的直接结果、耗时，定界、定位都需要较高的排查成本。

![image.png](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/v9kqDeaZ43xypOVx/img/60d71de8-7259-406e-8ad0-fb223238dae8.png)

聚焦到我们负责的沙箱控制面，一次 Sandbox 生命周期操作（claim / pause / resume / delete）横跨两个执行模型完全不同的组件：

| 组件 | 角色 | 执行模型 |
| --- | --- | --- |
| sandbox-manager | HTTP API 服务 | 同步，客户端直接调用 |
| agent-sandbox-controller | Kubernetes Operator | 异步，Reconcile 事件驱动 |

执行路径：`客户端 → manager → Sandbox CR → controller → Pod`

对照上图，这条路径上有三处断链：

1.  requestID 只是 manager 本进程的日志字段（uuid 格式），出不了进程，无法串联分析；
    
2.  Sandbox CR 的 annotation 不携带 trace 上下文，controller 的 Reconcile 无法归属到某次 API 调用；
    
3.  没有统一的 trace 后端，两个组件的日志落在各自的 stdout / SLS 里。
    

所以出问题时（Sandbox 启动慢、休眠唤醒卡住），只能分别翻两个组件的日志、按时间戳人工对齐，单组件问题分钟级起步，跨组件问题更不方便。

**目标**：解决上述三个问题，建设端到端全链路追踪

*   用一个 TraceID 把 manager 与 controller 串成完整调用链（**应对问题1**：X-Request-ID 从日志字段升级为 TraceID）
    
*   trace、日志、K8s 事件互相关联，CR annotation 承担跨组件传播（**应对问题 2**）
    
*   接入统一 trace ：早期用 OTel + Jaeger 快速跑通，后期改为 OTel + 阿里云 XTrace（**应对问题 3**）。下游 VK / Bazaar 等组件如果接入后自然在XTrace汇合
    
*   tracing 本身可控：设计 feature gate，用户可选 **otel（OTLP 上报）/ stdout / file / none（关闭）** 四种模式，关闭时零开销。
    

## 二、总体设计

![image.png](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/v9kqDeaZ43xypOVx/img/f3237c7f-6688-4187-b301-dd063a1449df.png)

**三个核心设计决策**：

1.  **每次操作都有X-Request-ID 直接作为 TraceID**
    
    *   每一次 E2B 操作，**无论成功失败**，都返回一个 requestID = TraceID
        
    *   查询该 ID：可以看 trace 瀑布图，可以 grep 两个组件的日志，同键关联无需转换
        
2.  **CR annotation 作为跨组件传播载体**
    
    *   manager 与 controller 之间没有直接调用，中间隔着 Kubernetes CR
        
    *   所有 W3C traceparent 写入 `agents.kruise.io/trace-context` annotation，Controller Reconcile 时提取
        
3.  **日志与 trace 双向打通**
    
    *   两个组件的日志都注入 traceID 字段
        
    *   一个 TraceID 既能看 timeline，也能查找到全部相关日志
        

---

## 三、关键技术难点

### 难点 1：同步 API vs 异步 Reconcile 的链路归属

**问题**：HTTP 请求同步有始有终；Reconcile 异步事件驱动、一次变更可能触发多次。传统 RPC 的"调用方带 context 调被调方"不成立。

**解决**：

*   manager 在**写 CR 的那一刻**把 trace context 写进 annotation 
    
*   controller 每次 Reconcile 从 annotation 提取 context，span 挂到同一条 trace 下
    
*   claim 这类同步等结果的流程：controller span 在时间轴上刚好嵌在 manager span 内部
    

### 难点 2：Reconcile 噪声控制

**问题**：status 更新、resync、无关事件都会触发 Reconcile。每次都产生 span，导致排查时打开 trace 看到几十条无意义的记录，真正需要被排查的trace很难被查询到，本质是**排查效率问题**。

**解决**：FilteringSpanProcessor + 写跟踪 client

*   Reconcile 开始时在 ctx 挂 write flag，K8s 写操作经过 write-tracking client 自动置位
    
*   结束时：**有写操作或有失败才保留 span**，纯读空转直接丢弃
    

**为什么选"是否发生写"作判断标准**：写操作是控制面产生实际影响的唯一途径。

---

## 四、最终效果

**适用场景一：sandbox唤醒链路时间异常排查**

Sandbox 被用户大批量唤醒时：可以看到唤醒耗时的 P99/P95/P90 分位数。

按耗时排序，直接拿到报错或长尾请求的 traceID，可以去继续排查：

![image.png](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/v9kqDeaZ43xypOVx/img/78de758d-7297-40d0-b993-391c5d429888.png)

**适用场景二：创建、休眠、唤醒、删除等失败问题排查**

发现失败的trace，并拿到对应的traceID，根据该traceID去对应的manager侧或者controller侧查具体日志，定位报错原因

![image.png](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/v9kqDeaZ43xypOVx/img/6920e6a4-d2cb-45b9-8df9-5d3ffab81b22.png)

## 五、收获

1.  **对ACS实例启动链路和休眠唤醒链路的理解加深** 一条 trace 完整呈现 `POST /sandboxes → manager.ClaimSandbox → infra.ClaimSandbox` + controller 侧 `Reconcile → PatchPod → updateSandboxStatus`。借此发现非直觉行为：预热池 claim 也会触发 controller 的 PatchPod（owner label / timeout 写入改变 spec hash）。所以 tracing 不只用于排障，也在帮我们理解系统自身
    
2.  **上下游：阿里云 VK、Bazaar 团队正在尝试 follow 同一套 tracing 思路**
    
    *   跨团队排查：把所有人拉进一个会，同一个 requestID 在不同组件里有不同的表达方式，做各种trace编号转换才能共同定位到一个问题
        
    *   上下游用**同一个 TraceID** 串起来，各团队拿着同一个 ID 跨组件共同定位，这也是我们做全链路 tracing 的初衷
        
3.  **社区与文档产出**
    
    *   参加 GitHub 社区双周例会：讲解设计思路并展示 Demo
        
    *   文档产出：《云原生 XTrace 配置 SOP》、《基于 OTel 的沙箱生命周期分布式追踪 v1.0 / v2.0》
        

---

# 工作2、SUO 原地升级与多 SUO 并发建设

---

## 一、业务背景与目标

### 业务背景：serving 沙箱需要受控的运维升级

平台的 serving 类业务（OpenClaw 这类长驻 Agent 服务）以**有状态、长生命周期**的沙箱为载体：用户会话、文件系统、运行中的进程都在沙箱里。沙箱一旦销毁，用户状态和数据会丢失。所以镜像打安全补丁、调资源规格这类日常变更，必须在**保留状态的前提下受控进行**。当前社区sandbox的 serving / long-running 配套的运维能力仍有不足：沙箱的创建、分配、回收已经比较完善，"存量有状态的沙箱怎么批量升级、怎么安全变更"没有现成方案。

**如果用传统 K8s 的升级来做**：**Deployment / StatefulSet 的滚动升级面向"可替换副本"**：升级的本质是销毁旧 Pod、拉起新 Pod，前提是副本同质、无个体状态。沙箱serving业务中每个sandbox是**不可替换的个体**，删 Pod 会丢用户状态。

所以需要一个**面向存量有状态沙箱的批量升级 API**：声明式选中一批沙箱，控制器在滚动窗口内受控推进，每个沙箱走完整的升级生命周期——这就是平台已有的 **SUO** 体系：

![image.png](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/v9kqDeaZ43xypOVx/img/7b5750b0-a82a-4025-aed1-704a2515ac81.png)

**SUO（SandboxUpdateOps）**：声明"把这一批 Sandbox 带到某个目标 spec"，控制器在滚动窗口内推进；每个沙箱依次经历 PreUpgrade（备份）→ UpgradePod（重建底层 Pod）→ PostUpgrade（恢复）的受控流程，失败有明确的失败状态可观测。本次的两项工作：**原地升级策略、多 SUO 并发**，都是在这个已有体系之上，围绕 serving 场景的运维需求做的建设。

### 两个痛点

**痛点 1：非原地升级如果想保证Sandbox数据持久化，所需的操作复杂且用户使用门槛较高**

现有升级策略以销毁 Pod 为代价：Pod 重建，emptyDir 上的数据随之清除。想保留状态只能靠 PreUpgrade/PostUpgrade 钩子把数据备份到 OSS/NAS、新 Pod 起来后再恢复。数据要出 Pod 走一次网络往返，量大时备份恢复耗时和网络带宽都是瓶颈。对 serving 沙箱，哪怕只换一个镜像、resize资源，或者主业务容器不动想升级 sidecar，也要付出"服务中断 + 整份数据搬迁"的代价。

**痛点 2：同 namespace 下最多只支持一个 Updating 的 SUO**

当前在同一个命名空间内只支持一个运行的 SUO，需等待前一个 SUO 完成或删除后才能创建新的 SUO。CI/CD 流水线、多人操作的并发提交因此无法受控落地；最理想情况下肯定是用户按序依次SUO升级，但是我们也要考虑并发的情况，比如多个管理员对同namesapce执行SUO，或者紧急修复的场景：想用修正版覆盖一个正在跑的错误升级目前做不到，只能等待或者手动删除旧的SUO。

### 目标

*   新增 `InplaceUpdate` 策略：只 patch 镜像/资源，Pod 不删、IP 不变；用户把数据挂在 emptyDir 上就随 Pod 原地保留，不需要 OSS/NAS 备份恢复的网络往返
    
*   多个 SUO 可以安全并发：每个沙箱**收敛**到用户最新意图，被取代的工作可见、不静默丢弃
    

---

## 二、总体设计与思考

### InplaceUpdate：支持原地升级

**核心思路：把重建的单位从 Pod 降到容器**。K8s 允许直接修改运行中 Pod 的容器 image 和 resources 字段，kubelet 会在原 Pod 内完成容器的替换。InplaceUpdate 就建立在这个语义上：升级不再删除重建 Pod，而是由 controller 对 Pod 做一次最小 diff patch，只把需要变的字段改掉。

*   **Pod 对象不删除**：UID、IP、调度结果、volume 挂载全部不变；emptyDir 的生命周期跟随 Pod，容器重建不清除，用户数据原地保留、零搬迁，不再需要 OSS/NAS 备份恢复的网络往返
    
*   **容器级精确重建**：patch 只包含发生变化的容器，kubelet 只对被改镜像的那一个容器"停旧起新"，同 Pod 内镜像未变的容器进程不中断、rootfs 原样保留。典型收益是 sidecar 升级：只重建 sidecar 容器，用户的业务主容器全程无感知
    

![image.png](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/v9kqDeaZ43xypOVx/img/9def366c-58b7-4a14-b4ed-ee928c4960f9.png)

**实现要点：**

**1、原地升级走完整的升级生命周期**：升级中沙箱 Phase=Upgrading、Ready=False，sandbox-manager 不会把它分配给新用户，运维过程可见、可隔离，隔离能力由 phase 语义直接保证，不需要额外机制。PreUpgrade/PostUpgrade 钩子照常执行，排水、健康检查有统一入口；完成/失败统一走 Upgrading condition 上报，SUO 侧一套状态分类代码覆盖所有策略

**2、换镜像时：controller 只 patch** `**pod.spec.containers[x].image**` **字段**。kubelet 的 syncLoop 观察到 image 变化 → 停掉该容器 → 在**同一个 Pod sandbox** 里用新镜像重建容器。Pod sandbox 无需销毁，所以 IP 不变、volume（emptyDir 数据）不变、env 和同 Pod 的其他容器全都不变。典型场景是 sidecar 升级：只重建 sidecar 容器，用户的业务主容器全程无感知

**3、只调资源时：走 K8s 原生 in-place resize 机制**，kubelet 直接调整容器的 cgroup 配额，不重启容器，进程完全无感。唯一的约束是 resize 不能改变 Pod 的 QoS class，controller 在下发前预检，不满足的立即判失败并上报原因，不会下发这种完不成的 resize 卡住滚动窗口

**4、不可恢复的失败快速终止**：ImagePullBackOff 这类拉不到镜像的错误，kubelet 会保持旧容器运行并无限重试；controller 识别后立即判为 terminal、在 condition 上报明确的失败原因，而不是让这个沙箱一直占着 maxUnavailable 滚动窗口，把整批升级挂住

### 多 SUO 并发：支持template模式升级

**为什么必须支持并发**：SUO 是一次性的运维对象，一次批量升级跑到终态就结束。但运维是长期的：用户会随时间不断提交新 SUO，上一批还在运行、下一版镜像到来，时序上重叠难以避免。所以**多 SUO 并发**是长期运维的常态，必须给出明确语义。

**现在 patch 模式很难支持多 SUO 场景**：存量 SUO 用 `spec.patch` 表达变更，声明的是叠加在沙箱当前状态上的**增量**。单发、串行的场景它完全够用，但放到多 SUO 并发下产生两个问题：

*   **增量之间比不出新旧，唯一安全的执行方式是严格串行**。patch 只描述差量、不描述终态：用户先提一个 patch 改 image，紧接着再提一个 patch 改 CPU 规格，期望的终态是"新镜像 + 新规格"——但这两个 patch 谁也不包含谁，跳过任何一个，它携带的变更就永久丢失；乱序应用，结果取决于时序。想不丢用户意图，只能严格按提交顺序逐个执行。而严格串行代价很大：Controller 的 reconcile 是无序、并发、可重入的事件模型，要在它之上为多个 SUO 维护一条全局严格队列，实现复杂且脆弱；排队期间一个卡住的沙箱会 park 整个批次，错误 SUO 卡住时连修复它的升级都进不来；每个沙箱还要强制依次滚过所有过时的中间版本。
    
*   **对用户不友好，容易写错**。strategic merge patch 要求用户懂 K8s 的合并语义，比如删一个 Volume 要写 `$patch: delete`、数组字段依赖隐蔽的 merge key，很容易提交一个"能通过校验但行为不符预期"的 SUO
    

**设计思路转变：让用户声明全量终态，而不是让系统比较增量**：这两个问题的根源都是 patch 的"增量"本性。于是我们反过来思考：能不能引入一个新模式，让用户直接写出完整的目标模板？这就是 `spec.template`/`templateRef`。全量模板就是**终态声明**，两个问题同时解决：

*   **新旧可比**：每个 template 都携带用户当时想要的完整最终状态，所以"以创建时间最新的 SUO 的 template 为准"就足够——它代表用户**最新的完整意图**，中间版本可以被安全跳过，不丢任何变更。并发语义合理的定为以最新 SUO 为准，因为它代表了用户最新的意图，不再需要全局队列。同时给出失败补救路径：升级出错，用户只需再提个修正版 SUO，它自动成为最新意图，不需要删旧 SUO 或等待
    
*   **模板好写**：用户照着期望的最终格式写即可，不需要任何 patch 指令，和创建 Sandbox/SandboxSet 用的是同一套模板结构，没有新的学习成本，对用户友好
    

后续更推荐用户使用 template 模式的 SUO ，存量 patch 保留为 legacy 串行模式兼容老用户，两种模式由 webhook 互斥，目前版本不会交错写同一批沙箱。

## 三、落地效果与产出

**场景 1：InplaceUpdate 不会销毁 Pod，留存Sandbox数据状态不需要走复杂的Recreate路径**

**以前想升级：**给 200 个 OpenClaw 沙箱换镜像版本。默认 Recreate 重建 Pod，rootfs/emptyDir 不保留，用户必须写 preUpgrade/postUpgrade 钩子，把工作目录备份到动态挂载的 OSS、升级后再恢复：

```yaml
apiVersion: agents.kruise.io/v1alpha1
kind: SandboxUpdateOps
metadata:
  name: upgrade-demo
spec:
  selector:
    matchLabels:
      agents.kruise.io/sandbox-template: openclaw-sbs
  updateStrategy:
    maxUnavailable: 10%
  patch:
    spec:
      containers:
      - name: gateway
        image: ghcr.io/openclaw/openclaw:2026.4.11
  lifecycle:
    preUpgrade:                # 升级前：打包工作目录，搬到 OSS
      exec:
        command: ["/bin/bash", "-c", "tar -czf /backup/openclaw-state.tgz -C /root .openclaw"]
      timeoutSeconds: 600
    postUpgrade:               # 升级后：从 OSS 搬回来解包恢复
      exec:
        command: ["/bin/bash", "-c", "tar -xzf /backup/openclaw-state.tgz -C /root"]
      timeoutSeconds: 600

```

数据出 Pod 走一次 OSS 网络往返，量大时备份恢复耗时和带宽都是瓶颈；升级失败重试还要手工剔除已执行的 preUpgrade、删除并重建 SUO。

**InplaceUpdate 之后**：Pod 不删、emptyDir 原地保留，备份恢复钩子整段删掉：

```yaml
apiVersion: agents.kruise.io/v1alpha1
kind: SandboxUpdateOps
metadata:
  name: openclaw-image-patch
spec:
  selector:
    matchLabels:
      agents.kruise.io/sandbox-template: openclaw-sbs
  updateStrategy:
    type: InplaceUpdate        # 原地升级：Pod 不删、IP 不变、emptyDir 数据原地保留
    maxUnavailable: 10%
  template:                    # 完整终态声明，声明"用户要的最终样子"
    spec:
      containers:
      - name: gateway
        image: ghcr.io/openclaw/openclaw:2026.4.12

```

Pod UID 不变、秒级生效；不再走 OSS/NAS 备份恢复的网络往返；`Upgrading` phase 全程可见，升级中的沙箱不会被分配出去。同一沙箱连续多轮升级、批量原地升级均验证通过。（需要排水、健康检查时，`lifecycle` 钩子在 InplaceUpdate 下照常可用，只是数据备份不再是必需操作）

一个典型场景是 **sidecar 升级**：平台要升级沙箱里注入的 agent-runtime / csi 这类 sidecar 时，走重建路径要销毁整个 Pod；InplaceUpdate 只重建 sidecar 那一个容器，**用户的业务主容器进程全程不中断**。

**场景 2：紧急修复从"等待或删除旧 SUO"变为单次操作，多 SUO 并发确定收敛到用户最新意图**

上面的升级滚到一半发现新版本镜像有问题——不用删旧 SUO、不用等，直接再提一个修正版：

```bash
$ kubectl apply -f ops-fix.yaml
# 同 selector，template 镜像改为修复版本

$ kubectl get suo
NAME                   PHASE       TOTAL   UPDATED   UPDATING   FAILED
openclaw-image-patch   Completed   200     120       0          0     # 其余 80 个：superseded by ops-fix
ops-fix                Updating    200     35        20         0

```

旧 SUO 的 status 显式记录 `120 updated, 80 superseded by ops-fix`——被取代的工作可见，不静默丢失；已升到旧目标版本的 120 个沙箱由 ops-fix 继续带到修复版本，最终全部收敛到最新意图，与两个 SUO 的执行时序无关。删除任何一个 SUO 同样安全：已落地的沙箱保持原样，永不回滚。

**社区与文档产出：**

两篇设计 proposal 和 specs（InplaceUpdate 策略、SUO并发协议）。

参加 GitHub 社区例会，讲解 SUO 的 InplaceUpdate 设计并给社区演示 Demo。

---

# 总结与展望

**两项工作的共同主线**：serving 沙箱是有状态、长生命周期的生产服务，平台要运营它需要两种基础能力。出了问题**看得见**（全链路 Tracing：一个 TraceID 贯穿 manager/controller 及上下游，分钟级定位），存量服务**改得动**（SUO：原地升级保数据不中断，多 SUO 并发确定收敛）。

两项工作补齐的都是沙箱平台从"能创建、能分配"到**"可观测、可运维"**的生产化能力。

**后续可能开展的工作方向**：

*   Tracing：推动更多上下游组件（比如ECS管控、运行时）接入统一 TraceID，覆盖 ACS 实例全链路
    
*   SUO：并发协议向更多运维场景（配置变更、批量回收）扩展